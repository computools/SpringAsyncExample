package org.mvv;

import org.mvv.configure.AsyncConfig;
import org.springframework.boot.SpringApplication;

public class AsyncApplication {

	public static void main(String[] args) {
		SpringApplication.run(AsyncConfig.class, args);
	}
}
